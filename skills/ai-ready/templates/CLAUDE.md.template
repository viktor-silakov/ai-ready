# CLAUDE.md Template

Generate customized version based on discovered project context.

```markdown
# CLAUDE.md - AI Assistant Guide for {project-name}

## Project Overview

{Brief description - 2-3 sentences about purpose}

## Tech Stack

- **Language:** {language} {version}
- **Framework:** {framework} {version}
- **Package Manager:** {npm/yarn/pnpm/pip/cargo}

## Project Structure

```
{project-root}/
├── src/              # Source code
├── tests/            # Test files
├── docs/             # Documentation
└── config/           # Configuration
```

## Common Commands

```bash
{install-command}     # Install dependencies
{dev-command}         # Run development
{test-command}        # Run tests
{build-command}       # Build for production
{lint-command}        # Lint code
```

## Code Conventions

- Files: {naming-convention}
- Functions: {naming-convention}
- Components: {naming-convention}

## Key Abstractions

### {Abstraction 1}
{What it does, when to use}

### {Abstraction 2}
{What it does, when to use}

## ALWAYS

- Write tests for new functionality
- Handle errors explicitly
- Use meaningful variable names
- Keep functions under 50 lines

## NEVER

- Commit secrets or API keys
- Use `any` without justification
- Skip error handling
- Create files over 500 lines

## ASK FIRST

- Deploying to production
- Deleting data or files
- Changing public APIs
- Adding new dependencies
- Database migrations

## Quick Reference

| Task | Command |
|------|---------|
| Install | `{install}` |
| Dev | `{dev}` |
| Test | `{test}` |
| Build | `{build}` |
```

## Template Variables

Replace these based on project analysis:
- `{project-name}` - From package.json name or directory
- `{language}`, `{version}` - Detected language
- `{framework}` - Detected framework
- Commands - From package.json scripts or Makefile
- Abstractions - Key patterns found in codebase
